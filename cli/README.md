# Orca CLI

The `orca` command will be the primary local administration interface for Orca OS.

Planned first commands:

- `orca status`
- `orca hardware`
- `orca nodes`
- `orca models`
- `orca services`
- `orca update`

v0.1 begins with `orca status` talking to the local `orcad` service.
