[Journal]
# Keep logs in RAM on embedded targets to avoid flash wear. The cap is small
# enough for a 64 MiB bring-up target while retaining useful recent context.
Storage=volatile
RuntimeMaxUse=4M
RuntimeKeepFree=2M
RuntimeMaxFileSize=1M
RateLimitIntervalSec=30s
RateLimitBurst=200
